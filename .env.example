# Optional server configuration for the authenticated IDFM PRIM connector.
# The application runs without it in labelled contract-replay mode.
# Never commit a real value or expose it as a VITE_* variable.
PRIM_API_KEY=
